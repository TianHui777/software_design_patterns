<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soft.dao.SOrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.soft.entity.SOrder">
        <id column="order_id" property="orderId" />
        <result column="destination" property="destination" />
        <result column="departure" property="departure" />
        <result column="time" property="time" />
        <result column="order_name" property="orderName" />
        <result column="payer_name" property="payerName" />
        <result column="cost" property="cost" />
        <result column="transport" property="transport" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        order_id, destination, departure, time, order_name, payer_name, cost, transport
    </sql>

</mapper>
